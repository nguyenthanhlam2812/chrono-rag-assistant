# ChronoRAG runtime dependencies (Sprint 0 -> Sprint 4 ML baseline).
# Versions pinned to the verified local Python 3.11 runtime so Sprint 4 ML
# metrics are reproducible across machines. Loosen with care: sklearn/numpy
# minor bumps can shift baseline F1 scores.

# --- Core ---
fastapi==0.124.2
uvicorn[standard]==0.38.0
pyyaml==6.0.2
python-dotenv==1.2.2
requests==2.32.3

# --- Preprocessing / ingestion ---
nltk==3.9.4
pymupdf==1.27.2.3
beautifulsoup4==4.14.2
lxml==6.0.2
# Preferred HTML main-content extractor (nav/sidebar/boilerplate removal).
# Optional: document_parser falls back to BeautifulSoup if it is absent.
trafilatura==2.0.0
# Optional heavy PDF backend with strong academic structure/table recovery.
# Enable via preprocessing.pdf_backend=docling; parser falls back to PyMuPDF
# if absent. Uncomment to install (pulls layout/table models on first run).
# docling>=2.0,<3.0

# --- ML / training (Sprint 4 ML baseline) ---
pandas==2.3.3
numpy==2.3.3
scikit-learn==1.8.0
joblib==1.5.3
matplotlib==3.10.6
plotly==6.7.0

# --- Retrieval / DL (Sprint 4 DL + Sprint 5, not yet used by pipeline) ---
torch==2.11.0
sentence-transformers==5.5.0
faiss-cpu==1.13.2
rank-bm25==0.2.2

# --- Optional LLM orchestration ---
# ChronoRAG runs without these packages. Set CHAT_ANSWERER=langchain to send
# chat-completion calls through LangChain before falling back to raw requests.
langchain>=1.1.0,<2.0.0
langchain-openai>=1.1.0,<2.0.0

# --- External eval (HuggingFace BEIR/SciFact for Recall@k benchmark) ---
datasets==5.0.0

# --- Dev / test ---
pytest==9.0.3
openpyxl>=3.1.5,<4.0.0
