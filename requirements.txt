# ChronoRAG runtime dependencies (Sprint 0 -> Sprint 4 ML baseline).
# Versions pinned to the verified local Python 3.11 runtime so Sprint 4 ML
# metrics are reproducible across machines. Loosen with care: sklearn/numpy
# minor bumps can shift baseline F1 scores.

# --- Core ---
fastapi==0.124.2
uvicorn[standard]==0.38.0
pyyaml==6.0.2
python-dotenv==1.2.2
requests==2.32.3

# --- Preprocessing / ingestion ---
nltk==3.9.4
pymupdf==1.27.2.3
beautifulsoup4==4.14.2
lxml==6.0.2

# --- ML / training (Sprint 4 ML baseline) ---
pandas==2.3.3
numpy==2.3.3
scikit-learn==1.8.0
joblib==1.5.3
matplotlib==3.10.6
plotly==6.7.0

# --- Retrieval / DL (Sprint 4 DL + Sprint 5, not yet used by pipeline) ---
torch==2.11.0
sentence-transformers==5.5.0
faiss-cpu==1.13.2
rank-bm25==0.2.2

# --- Optional LLM orchestration / router ---
# ChronoRAG runs without these packages. Set CHAT_ROUTER=langchain to let a
# local/OpenAI-compatible LLM classify ambiguous chat intents.
langchain>=1.1.0,<2.0.0
langchain-openai>=1.1.0,<2.0.0

# --- Dev / test ---
pytest==9.0.3
openpyxl>=3.1.5,<4.0.0
